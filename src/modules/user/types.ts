export enum UserOrderType {
    CREATED = 'createdAt',
    UPDATED = 'updatedAt',
}

export enum UserValidateGroups {
    CREATE = 'user-create',
    UPDATE = 'user-update',
    REGISTER = 'user-register',
}
